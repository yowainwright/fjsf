{
  "name": "fuzzy-npm-scripts",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "fnpm": "./src/cli.ts"
  },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build src/cli.ts --outdir ./dist --target bun --minify",
    "test": "bun test",
    "lint": "oxlint src",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
